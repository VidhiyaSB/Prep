class Solution {
    public String reverseWords(String s) {
         String arr[] = s.split(" ");
        int n = arr.length;
        String str="";
        for(int i=n-1;i>=0;i--){
         str += arr[i]+" ";  
         
        }
        return str.trim();
    }
}
